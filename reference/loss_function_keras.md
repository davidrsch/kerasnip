# Dials Parameter for Keras Loss Functions

Dials Parameter for Keras Loss Functions

## Usage

``` r
loss_function_keras(values = NULL)
```

## Arguments

- values:

  A character vector of possible loss functions. Defaults to all known
  losses (keras defaults + custom registered).

## Value

A `dials` parameter object for Keras loss.
