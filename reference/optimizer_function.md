# Dials Parameter for Keras Optimizers

Dials Parameter for Keras Optimizers

## Usage

``` r
optimizer_function(values = NULL)
```

## Arguments

- values:

  A character vector of possible optimizers. Defaults to all known
  optimizers (keras defaults + custom registered).

## Value

A `dials` parameter object for Keras optimizers.
