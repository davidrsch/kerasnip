# print method works

    Code
      print(rec$steps[[1]])
    Output
      Sliding window of predictors (unprepped)

---

    Code
      print(prepped_rec$steps[[1]])
    Output
      Sliding window (timesteps = 3 ) of predictors: x1  ->  sequence_matrix 

