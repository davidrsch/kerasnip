% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_view.R
\name{kerasnip_step_view}
\alias{kerasnip_step_view}
\title{View a Single Forecast Step of a Multistep kerasnip Fit}
\usage{
kerasnip_step_view(x, step, var = NULL)
}
\arguments{
\item{x}{A fitted (trained) \code{workflow} whose model is a multistep
regression model (see \code{create_keras_sequential_spec()}/
\code{create_keras_functional_spec()} with a vector-valued output).}

\item{step}{An integer, the forecast step to view.}

\item{var}{A string, the forecasted variable to view. Required only if
the model forecasts more than one variable; inferred otherwise.}
}
\value{
A \code{kerasnip_step_view} object.
}
\description{
A kerasnip multistep (vector-valued) regression model returns a nested
\code{.pred} list-column: one inner tibble per row, with a \code{.step} column plus
one prediction column per forecasted variable. \code{tailor}/\code{probably} expect
a single flat numeric \code{.pred} column instead — \code{tailor::check_variable_type()}
requires \code{is.numeric()} on the outcome/estimate columns, which a
list-column fails outright.

\code{kerasnip_step_view()} wraps a fitted multistep workflow together with
one forecast step (and, if more than one variable is forecast, which
variable), presenting it as an ordinary single-output fit: \code{predict()}
returns a flat \code{.pred} column for that step alone.
}
\details{
Unlike \code{\link[=kerasnip_output_view]{kerasnip_output_view()}}, a multistep model's per-step outcome
columns (e.g. \code{lead_1_value}) are recipe-\emph{engineered} from a single raw
column via \code{step_lead()} — they are not present in a user's raw data the
way genuine multi-output columns are. \code{\link[=kerasnip_step_truth]{kerasnip_step_truth()}} recovers
the true future value at a given step by re-baking the fitted recipe on
raw data, which is what \link[probably:int_conformal_split]{int_conformal_split()}
uses internally for this class.

\code{probably::int_conformal_full()} is not supported for step views: its
refit-per-candidate design would need to substitute a candidate value
into the single \emph{raw} column that \code{step_lead()} derives every step's
truth from, which then shifts every nearby row's target — a materially
different (and more involved) problem than the single-column substitution
\code{\link[=kerasnip_output_view]{kerasnip_output_view()}} uses, and is not implemented here.
}
\examples{
\dontrun{
fit_obj <- fit(wf, data = train_data) # a multistep forecasting workflow
step_2 <- kerasnip_step_view(fit_obj, step = 2)
predict(step_2, new_data = test_data) # -> a single `.pred` column
}
}
