% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess_intervals_classification.R
\name{laplace_conf_int_cls}
\alias{laplace_conf_int_cls}
\title{Predict Confidence Intervals for Classification via LLA}
\usage{
laplace_conf_int_cls(object, x, laplace_data, lvl, level = 0.95)
}
\arguments{
\item{object}{The raw Keras model (from \code{object$fit$fit}).}

\item{x}{Processed predictor data (matrix or array).}

\item{laplace_data}{A named list of Laplace posterior data (from
\code{object$fit$laplace}).}

\item{lvl}{Character vector of class level names (from
\code{object$fit$lvl}).}

\item{level}{Confidence level (default 0.95).}
}
\value{
For single-output: a data frame with per-class
\code{.pred_lower_Level} and \code{.pred_upper_Level} columns.
}
\description{
Predict-time entry point for classification confidence intervals.
Called by parsnip via
\code{c(pkg = "kerasnip", fun = "laplace_conf_int_cls")}.

For each output, this uses Monte Carlo sampling from the Laplace
posterior over logits, transforms to probability scale via sigmoid /
softmax, and returns per-class quantile-based intervals.
}
\keyword{internal}
