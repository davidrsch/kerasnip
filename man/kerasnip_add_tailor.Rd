% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kerasnip_add_tailor.R
\name{kerasnip_add_tailor}
\alias{kerasnip_add_tailor}
\title{Attach a \code{tailor} Post-Processor to One Output or Step of a Multi-Output
or Multistep Workflow}
\usage{
kerasnip_add_tailor(x, tailor, output = NULL, step = NULL, var = NULL)
}
\arguments{
\item{x}{An \strong{unfitted} \code{workflow} whose model has more than one outcome
(multi-output) or is a multistep forecasting model.}

\item{tailor}{A \code{tailor::tailor()} specification.}

\item{output}{A string, the name of the outcome column to post-process
(multi-output models).}

\item{step}{An integer, the forecast step to post-process (multistep
models).}

\item{var}{A string, the forecasted variable to post-process; only
needed with \code{step} if the model forecasts more than one variable.}
}
\value{
A \code{kerasnip_tailored_workflow}, to be trained with \code{fit()}.
}
\description{
\code{workflows::add_tailor()} cannot be used on a kerasnip multi-output or
multistep workflow: \code{tailor::fit()} selects \code{outcome}/\code{estimate} via
\code{[[}, which requires exactly one, flat, numeric column, and both a
multi-output recipe (\code{output_1 + output_2 ~ .}) and a multistep model's
nested \code{.pred} list-column violate that (see
\code{vignette("multi_output_postprocessing")}). \code{kerasnip_add_tailor()} is a
kerasnip-owned analogue that attaches a \code{tailor} post-processor to a
single named output or forecast step, using \code{\link[=kerasnip_output_view]{kerasnip_output_view()}} or
\code{\link[=kerasnip_step_view]{kerasnip_step_view()}} internally.
}
\details{
At \code{fit()} time, the underlying model is trained as usual; the relevant
view is then used to fit the \code{tailor} against that output's/step's
predictions (on \code{data_calibration} if supplied, otherwise on \code{data},
mirroring \code{workflows::add_tailor()}'s data-usage convention). At
\code{predict()} time, the full prediction is generated, the target
output's/step's value(s) are replaced with the tailor-adjusted values,
and everything else (other outputs; other steps in the same nested
tibble) is left untouched.

Exactly one of \code{output} or \code{step} must be supplied: \code{output} for a
multi-output model, \code{step} (and \code{var}, if more than one variable is
forecast) for a multistep model.
}
\examples{
\dontrun{
tlr <- tailor::tailor() |> tailor::adjust_numeric_calibration()

# multi-output
tailored_wf <- kerasnip_add_tailor(wf, tlr, output = "output_1")

# multistep
tailored_wf <- kerasnip_add_tailor(wf, tlr, step = 2)

fit_obj <- fit(tailored_wf, data = train_data, data_calibration = cal_data)
predict(fit_obj, new_data = test_data)
}
}
