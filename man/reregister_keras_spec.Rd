% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reregister_keras_spec.R
\name{reregister_keras_spec}
\alias{reregister_keras_spec}
\title{Re-register a Kerasnip Model Specification with Parsnip}
\usage{
reregister_keras_spec(x, env = parent.frame())
}
\arguments{
\item{x}{A kerasnip model specification (an object whose class inherits from
a registered kerasnip model), or a fitted \code{workflow} containing one.
The object must carry \code{kerasnip_layer_blocks} and \code{kerasnip_functional}
attributes — these are embedded automatically by
\code{create_keras_sequential_spec()} / \code{create_keras_functional_spec()} in
kerasnip >= 0.1.1.}

\item{env}{The environment in which to (re-)create the named constructor
function (e.g. \code{basic_mlp}). Defaults to the calling environment.}
}
\value{
Invisibly returns the model name (a character string). Called
primarily for its side effects.
}
\description{
Restores the parsnip model registration for a kerasnip model specification
after it has been loaded into a new R session.
}
\details{
\code{create_keras_sequential_spec()} and \code{create_keras_functional_spec()} work by
registering a new model type with parsnip via side-effect calls to
\code{parsnip::set_new_model()}, \code{parsnip::set_fit()}, etc. These registrations are
stored in parsnip's internal session environment and are \strong{not} serialized
when a model or workflow is saved with \code{\link[=saveRDS]{saveRDS()}} or \code{bundle::bundle()}.

When a kerasnip workflow is loaded in a new R session, calling \code{predict()}
will fail with:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Error in `get_encoding()`: ! Model "your_model" has not been registered.
}\if{html}{\out{</div>}}

\code{reregister_keras_spec()} solves this by reading registration metadata that
is embedded in every kerasnip spec object (since v0.1.1) and replaying the
parsnip registration calls. It also re-creates the named constructor function
(e.g. \code{basic_mlp()}) in the calling environment.
\subsection{Recommended save/load workflow}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Session 1 — save
library(kerasnip)
library(bundle)

create_keras_sequential_spec("basic_mlp", layer_blocks = ..., mode = "regression")
wf      <- workflow() |> add_model(basic_mlp()) |> add_formula(y ~ .)
fit_wf  <- fit(wf, data = train_data)
bundled <- bundle(fit_wf)
saveRDS(bundled, "model.rds")

# Session 2 — load
library(kerasnip)
library(bundle)

bundled  <- readRDS("model.rds")
fit_wf   <- unbundle(bundled)            # bundle restores the Keras model
reregister_keras_spec(fit_wf)            # kerasnip restores parsnip registration
predict(fit_wf, new_data = test_data)    # works
}\if{html}{\out{</div>}}

If you do not need to save the fitted Keras model weights (e.g. for testing
or sharing a spec only), you can skip \code{bundle} and use plain \code{\link[=saveRDS]{saveRDS()}}
combined with \code{reregister_keras_spec()} to restore the parsnip-only side.
}
}
\seealso{
\code{\link[=create_keras_sequential_spec]{create_keras_sequential_spec()}}, \code{\link[=create_keras_functional_spec]{create_keras_functional_spec()}},
\code{\link[=remove_keras_spec]{remove_keras_spec()}}
}
