% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_view.R
\name{kerasnip_step_truth}
\alias{kerasnip_step_truth}
\title{Recover Truth Values for a Multistep Forecast Step}
\usage{
kerasnip_step_truth(view, new_data)
}
\arguments{
\item{view}{A \code{kerasnip_step_view}.}

\item{new_data}{A data frame of raw predictors (and the original outcome
column \code{step_lead()} was applied to).}
}
\value{
A numeric vector, one truth value per row of \code{new_data}.
}
\description{
A multistep model's per-step outcome columns (e.g. \code{lead_2_value}) are
engineered by \code{step_lead()} from a single raw column, so they are not
present in a user's raw data the way genuine multi-output columns are.
This re-bakes the fitted recipe on \code{new_data} to recover the actual
future value at \code{\link[=kerasnip_step_view]{kerasnip_step_view()}}'s step, for calibration/interval
use. Rows too close to the end of \code{new_data} for the lead to be computed
return \code{NA} (dropped automatically by calibration routines that call
\code{sort()}/\code{stats::complete.cases()} on the result).
}
