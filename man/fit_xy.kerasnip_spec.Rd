% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kerasnip_spec_methods.R
\name{fit_xy.kerasnip_spec}
\alias{fit_xy.kerasnip_spec}
\title{fit_xy Method for kerasnip Spec Objects}
\usage{
\method{fit_xy}{kerasnip_spec}(object, ...)
}
\arguments{
\item{object}{A \code{kerasnip_spec} model specification.}

\item{...}{Passed to \code{parsnip::fit_xy.model_spec()}.}
}
\value{
A \code{model_fit} object with the additional \code{kerasnip_model_fit} class
prepended to its class vector.
}
\description{
S3 method for \code{fit_xy()} dispatched on \code{kerasnip_spec} objects. Workflows
route through \code{fit_xy} rather than \code{fit}, so this method ensures the
\code{kerasnip_model_fit} class is attached in the workflow fitting path as well.

\code{kerasnip_spec} is stripped from the class before \code{NextMethod()} to prevent
parsnip's internal \code{specific_model()} helper from returning more than one
model-class entry, which would break registry lookups. The custom metadata
attributes remain on the object and are thus stored inside the resulting
\code{model_fit$spec}.
}
\keyword{internal}
