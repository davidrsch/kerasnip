% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_view.R
\name{augment.kerasnip_step_view}
\alias{augment.kerasnip_step_view}
\title{Augment Method for \code{kerasnip_step_view} Objects}
\usage{
\method{augment}{kerasnip_step_view}(x, new_data, ...)
}
\arguments{
\item{x}{A \code{kerasnip_step_view}.}

\item{new_data}{A data frame of raw predictors (and the original outcome
column \code{step_lead()} was applied to).}

\item{...}{Not used.}
}
\value{
A tibble: \code{.pred}, the step's truth column (named
\code{x$outcome_col}), and \code{new_data}'s columns, aligned to the rows that
survived windowing.
}
\description{
Binds \code{predict(x, new_data, type = "numeric")}'s \code{.pred} column with the
step's truth (from \code{\link[=kerasnip_step_truth]{kerasnip_step_truth()}}) and \code{new_data}, mirroring
\code{workflows:::augment.workflow()}. Used internally by
\code{int_conformal_split.kerasnip_step_view()}. Rows \code{step_sequence()} drops
for lacking a full window of history are dropped here too, to stay
aligned with \code{predict()}'s row count.
}
\keyword{internal}
