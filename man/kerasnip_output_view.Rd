% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output_view.R
\name{kerasnip_output_view}
\alias{kerasnip_output_view}
\title{View a Single Output of a Multi-Output kerasnip Fit}
\usage{
kerasnip_output_view(x, output)
}
\arguments{
\item{x}{A fitted (trained) \code{workflow} whose model has more than one
outcome column.}

\item{output}{A string, the name of the outcome column to view.}
}
\value{
A \code{kerasnip_output_view} object.
}
\description{
\code{tailor} and \code{probably} are built around models with a single outcome
column and a single \code{.pred}/\code{.pred_class} prediction column. A kerasnip
multi-output model (e.g. a recipe with \code{output_1 + output_2 ~ .}) instead
produces \code{.pred_output_1}, \code{.pred_output_2}, ... columns from multiple
truth columns in one fit — the standard \code{parsnip::maybe_multivariate()}
shape, but one \code{tailor::fit()}/\code{workflows::add_tailor()} call cannot
consume it (it selects \code{outcome}/\code{estimate} via \code{[[}, which requires
exactly one column).

\code{kerasnip_output_view()} wraps a fitted multi-output workflow together
with one output name, presenting it as if it were an ordinary
single-output fit: \code{predict()} returns standard \code{.pred} / \code{.pred_class} /
\verb{.pred_<level>} columns for that output alone, letting you calibrate or
post-process each output separately with the usual \code{tailor}/\code{probably}
calls (see \code{vignette("multi_output_postprocessing")}).
}
\examples{
\dontrun{
fit_obj <- fit(wf, data = train_data) # wf predicts output_1 and output_2
view_1 <- kerasnip_output_view(fit_obj, "output_1")
predict(view_1, new_data = test_data) # -> a single `.pred` column
}
}
