% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess_intervals_regression.R
\name{laplace_pred_int_reg}
\alias{laplace_pred_int_reg}
\title{Predict Prediction Intervals via Last-Layer Laplace Approximation}
\usage{
laplace_pred_int_reg(object, x, laplace_data, level = 0.95)
}
\arguments{
\item{object}{The raw Keras model (from \code{object$fit$fit}).}

\item{x}{Processed predictor data (matrix or array).}

\item{laplace_data}{A named list of Laplace posterior data, one entry per
output (from \code{object$fit$laplace}).  Each entry contains \code{h_diag}, \code{tau},
\code{sigma_sq_noise}, \code{n_training}, and \code{combined_model}.}

\item{level}{Confidence level (default 0.95).  Passed through from
\code{predict(..., type = "conf_int", level = 0.95)}.}
}
\value{
A named list of matrices (one per output), each with columns
\code{.pred}, \code{.pred_lower}, and \code{.pred_upper}.
}
\description{
Predict-time entry point for regression prediction intervals.  Called by
parsnip via
\code{c(pkg = "kerasnip", fun = "laplace_pred_int_reg")}.

For each output in the model, this builds the per-sample predictive variance
(uncertainty on a new observation \code{Y|X} = epistemic variance + observation
noise) from the stored Laplace posterior and returns symmetric Normal-based
intervals at the requested level.
}
\keyword{internal}
